#!/usr/bin/env perl

use Rex::ShellExtension;

my $command = "run";

my $r = Rex::ShellExtension->new;

my $exit_code = 0;

unshift @ARGV, "test";

print "" . $r->call($command, join_argv => " ") . "\n";

exit $?;
